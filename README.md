# jdbc_repository

A Java JDBC project demonstrating how to connect Java applications with a MySQL database and perform complete database operations including **CRUD (Create, Read, Update, Delete)**, **transactions**, **batch processing**, and **image handling**.

---

## 🚀 Features
- ✅ Establish and close database connections
- ✅ Insert, Update, Delete, and **Retrieve (Select)** records
- ✅ Use of `PreparedStatement` for secure queries
- ✅ Transaction management (Commit & Rollback)
- ✅ Batch processing for multiple queries
- ✅ Store and retrieve images using BLOB data type
- ✅ Proper exception handling and resource cleanup

---

## 🧠 Technologies Used
- **Java**
- **MySQL Database**
- **JDBC (Java Database Connectivity) API**


   ```bash
   git clone https://github.com/your-username/JDBC-Database-Repository.git
